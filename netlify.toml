[build]
  command = "npm run build"
  publish = "dist"

# Regular Netlify Function (Node.js) handles /api/claid/* via export const config in the .mjs file
# Edge function removed — use process.env instead of Deno.env for reliability
