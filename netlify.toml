[build]
  command = "npm run build"
  publish = "dist"

# Edge Function: proxy /api/claid/* to Claid.ai API server-side (avoids CORS)
[[edge_functions]]
  path = "/api/claid/image/edit/upload"
  function = "claid-proxy"
